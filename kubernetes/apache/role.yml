kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: apache-manager
  namespace: apache
rules:
  - apiGroups: ["","apps"]#batch,etc
    resources: ["pods","deployments","services"]#hpa,vpa
    verbs: ["get"]# watch,delete,apply
    
